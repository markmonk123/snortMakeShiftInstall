# Environment secrets for ML-Enhanced Snort
# Rename this file to ".env" (or export the variables manually) and fill in
# real values before running the container or helper scripts.

# Primary OpenAI credential consumed by docker-entrypoint.sh and setup_openai_api.sh
e

# Optional alias recognized by setup scripts
OPENAI_API_KEY=${ML_API_KEY}

# Model configuration defaults used by the ML runner when sourcing the env file
ML_MODEL_TYPE=openai
ML_MODEL_NAME=gpt-4
CONFIDENCE_THRESHOLD=0.98
